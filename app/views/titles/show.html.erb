<div class="container mt-200">
	<h2 class="text-center"> Title </h2>
	<div class="row text-center">
		<div class='col-md-3'>
		</div>
			<div class="col-md-6">
				<p id="notice"><%= notice %></p>

					<p><strong>Title:</strong>
					<%= @title.name %></p>
							<%= link_to 'Edit', edit_title_path(@title), class: 'btn btn-danger' %>
		<%= link_to 'Back', titles_path, class: 'btn btn-secondary' %>
			</div>
		<div class='col-md-3'>
		</div>

	</div>
</div>